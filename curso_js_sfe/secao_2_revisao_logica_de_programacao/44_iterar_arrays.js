const nomes = ['Joao', 'Maria', 'Jose', 'Helena']; // Array de nomes

// Loop for que itera sobre o array de nomes
for (let i = 0; i < nomes.length; i++) {
    console.log(`${i}: Nome é ${nomes[i]}`); // Imprime o índice e o nome correspondente
}
